import { InsertDynamicDirective } from './insert-dynamic.directive';

describe('InsertDynamicDirective', () => {
  it('should create an instance', () => {
    const directive = new InsertDynamicDirective();
    expect(directive).toBeTruthy();
  });
});
